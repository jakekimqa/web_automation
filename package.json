{
  "name": "playwright-ai-vibe",
  "version": "1.0.0",
  "private": true,
  "description": "Fresh Playwright project for AI-powered web automation proof",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0"
  }
}
